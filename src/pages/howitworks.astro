---
import EntryLayout from "@components/portfolio/EntryLayout.astro";
import { getIndex } from "@lib/contentParser";

const entry = (await getIndex("portfolio")) as PortfolioEntry;

---

<EntryLayout entry={entry} />